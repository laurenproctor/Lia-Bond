# Prompt 3 — Build administration and intelligence views

Fully implement:

- `/insights`
- `/locations`
- `/rules`
- `/integrations`
- `/brand-voice`
- `/settings`

Use the corresponding reference images.

Requirements:
- Charts should use a reusable chart library or lightweight SVG components.
- Rules builder should support adding/removing mock conditions and actions.
- Brand voice sliders must update the live response preview.
- Integration cards must show explicit capability states.
- Location selection should update the right-side detail panel.
- Settings changes should use local form state and a save confirmation.

Run lint, type-check, and build. Fix all errors.
